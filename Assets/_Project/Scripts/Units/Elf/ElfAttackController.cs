using System;
using UnityEngine;

public class ElfAttackController : MonoBehaviour
{
    private void OnTriggerEnter2D(Collider2D other)
    {
        // if(TryGetComponent(out var PlayerHealthController))
    }
}
